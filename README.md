# day-15---Working-with-CSV-files
Whenever we get some csv dataset, it is very messy and complicated. To handle this or to convert the data in DataFrame we use pandas. In this repo. we have done this.

pd.read_csv()
its parameters like: sep, names, index_cols, header, usecols, skiprows, nrows, encoding, on_bad_lines, dtype, parse_dates, converters, na_values, chunksize
